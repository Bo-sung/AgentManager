# Worker prompt templates

Reserved for future `--prompt-template` snippets specific to the Worker role.
Empty for the initial version — Worker directives are currently injected
directly by `extensions/worker-guard.ts` via `before_agent_start`, and the
report format is duplicated in `skills/worker-task/SKILL.md` for discoverability.
